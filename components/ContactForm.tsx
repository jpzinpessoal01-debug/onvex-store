"use client";
import { FormEvent, useState } from "react";
import { useToast } from "./ToastProvider";
export function ContactForm(){const[loading,setLoading]=useState(false);const{showToast}=useToast();const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setLoading(true);const form=event.currentTarget;const payload=Object.fromEntries(new FormData(form).entries());const response=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const result=await response.json() as {error?:string};if(response.ok){form.reset();showToast("Mensagem recebida. Nosso time responderá em breve.");}else showToast(result.error??"Não foi possível enviar sua mensagem.","error");setLoading(false);};return <form className="contact-form" onSubmit={submit}><div className="form-grid"><label>NOME<input name="name" required/></label><label>E-MAIL<input type="email" name="email" required/></label><label>TELEFONE<input name="phone"/></label><label>ASSUNTO<select name="subject" required><option value="Pedido">Pedido</option><option value="Troca ou devolução">Troca ou devolução</option><option value="Produto">Produto</option><option value="Parceria">Parceria</option><option value="Outro">Outro</option></select></label><label className="full">MENSAGEM<textarea name="message" rows={7} required/></label></div><button className="button button--silver" disabled={loading}>{loading?"ENVIANDO…":"ENVIAR MENSAGEM →"}</button></form>}

